spring:
  application:
    name: pokedex

  # Server configuration
  server:
    port: 8080

# PokeAPI configuration
pokeapi:
  base:
    url: https://pokeapi.co/api/v2

# Logging
logging:
  level:
    root: INFO
    com.homechallenge: DEBUG

